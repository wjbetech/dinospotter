# Issue tracker: GitHub Issues (`gh`)

- Repo: `wjbetech/dinospotter`
- Work: `gh issue create/list/view` + PRs.
- Labels: `needs-triage`, `ready-for-agent`, `ready-for-human`, `wontfix`.
- Local scratch (only pre-spec): `.scratch/<feature>/`
- The roadmap's §6 table is the local dependency plan, not proof that GitHub
  issues exist. Publish issues only after T0.0 and the named D4-D7 decisions
  are recorded; copy the exact blocker, DoD, check, and perf gate into each
  issue. Keep GitHub status and the roadmap's frontier aligned.
