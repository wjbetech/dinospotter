# ADR-003: Own flat silhouette set (resolves quirks 6, D2 closed)

Status: accepted 2026-09-06.
Decision: 8 hand-drawn flat `ink #1E242B` clade SVGs (theropod, sauropod, ornithischian, pterosaur, marine, synapsid, amphibian, fallback) on `sand` grid; tick-bar scale in cards, human figure in modal only.
Keyword map order: ptero → thero/rex/raptor → titan/saur → cerat/hadro/steg/ankyl → marine → therapsid/synap → amphib → fallback. Unknown → fallback (pinned).
Why: PBDB has no images; own art = zero licensing risk; flat fill stays crisp at card size (style Q5–Q6 locked).
Files: `apps/website/public/sprites/*.svg` + sprite sheet, lazy below fold. Clade list final — no amendment expected.
